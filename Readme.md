# Task: CSS - Laundry Services Hero Section

## Description
This project implements the Hero section and Navigation bar for a laundry service web application. The layout is built strictly using CSS display inline properties and viewport units, completely avoiding the use of flexbox to ensure precise screen fitting without scrolling.